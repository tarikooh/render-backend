info:
  name: Unknown Endpoint
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:3001/what
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
