info:
  name: Delete Notes
  type: http
  seq: 4

http:
  method: DELETE
  url: http://localhost:3001/api/notes/5
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
