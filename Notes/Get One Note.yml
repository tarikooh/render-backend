info:
  name: Get One Note
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:3001/api/notes/2
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
